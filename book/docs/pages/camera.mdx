# Camera

Left click pans the camera around the board. 

Right click changes the altitude of the camera.

Press `🔭` to reset the camera to a top down, north facing view.

Press `🧭` to rotate the board in 45 degree increments.
