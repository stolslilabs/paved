import { Builder } from "@paved/game-core";
import type { ChainClient } from "../client";
export declare function useBuilder(client: ChainClient | null, gameId: number | null, playerId: string | null): {
    builder: Builder | null;
};
//# sourceMappingURL=useBuilder.d.ts.map