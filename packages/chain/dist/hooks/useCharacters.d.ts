import { Character } from "@paved/game-core";
import type { ChainClient } from "../client";
export declare function useCharacters(client: ChainClient | null, gameId: number | null, playerId: string | null): {
    characters: Character[];
};
//# sourceMappingURL=useCharacters.d.ts.map